__pycache__/
*.pyc
.DS_Store
.venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Belt-and-braces: no stray PDFs in the repo root, but bundled reference
# material under references/ is tracked on purpose (interop plan, FAQ, and
# any future level-specific normative PDFs).
*.pdf
!references/*.pdf

# Claude Code session state — per-user, never tracked.
.claude/

# Local announcement / message-board drafts — scratch only, never tracked.
message.txt
message*.txt
